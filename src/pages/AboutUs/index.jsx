import React, { useState } from "react";
import TheAboutUs from "./components/TheAboutUs";

const AboutUs = ({ Logout, UserToken }) => {
  return (
    <div>
      <TheAboutUs />
    </div>
  );
};

export default AboutUs;
